import { lessThan } from '../../../helpers/less-than';
import { module, test } from 'qunit';

module('Unit | Helper | less than');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = lessThan(42);
  assert.ok(result);
});
